<div class="col-sm-12 alert alert-info" id="standard_product_notice">
  <strong>No colors or sizes are set for this product yet.</strong> You can initialize the color/size set by selecting which colors and
  sizes you'd like to add. You can individually add and remove colors and sizes after.
  <% #TODO: should the text be in a locale? %>
</div>

<% #TODO: Model calls? %>
<div class="form-group">
  <div class="form-group clearfix">
    <%= f.label :color_ids, 'Colors', class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= collection_select 'color', 'ids', Color.all, :id, :name, {}, { multiple: true, data: { placeholder: 'Select Colors' } } %>
    </div>
  </div>

  <div class="form-group clearfix">
    <%= f.label :size_ids, 'Sizes', class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= collection_select 'size', 'ids', Size.all, :id, :name, {}, { multiple: true, data: { placeholder: 'Select Sizes' } } %>
    </div>
  </div>

</div>
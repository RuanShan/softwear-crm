<%= form_for(@imprintable) do |f| %>
    <% if @imprintable.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@imprintable.errors.count, 'error') %> prohibited this product from being saved:</h2>

          <ul>
            <% @imprintable.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :catalog_number %><br>
      <%= f.text_field :catalog_number %>
    </div>
    <div class="field">
      <%= f.label :description %><br>
      <%= f.text_area :description, rows: 6 %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>

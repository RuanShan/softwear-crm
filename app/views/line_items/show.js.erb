var $row = $("#line-item-<%= @line_item.id %>");
var $oldChildren = $row.children();

$oldChildren.each(function() {
  $(this).remove();
});

$row.append("<%= j render partial: 'standard_view_entry', locals: { line_item: @line_item } %>");

updateOrderTimeline();

<% if objects.empty? %>
	<p>Uh oh! Couldn't find any <%= type_name.humanize.pluralize %> that fit the given criteria.</p>
<% else %>
	<%= select_tag type_name.underscore+'_id', options_from_collection_for_select(objects, 'id', 'name') %>
<% end %>
setupContentModal(function(modal) {
  $('#contentModal .modal-dialog').css('width', '75%');
});
showContentModal({
  body: $("<%=j render('quotes/edit/new_line_item', quote: @quote) %>"),
  then: function() {
    var form = $('#new-group-form');
    registerImprintEvents(form);
  }
});

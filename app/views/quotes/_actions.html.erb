<div class="box-info col-xs-12">
  <h3><strong>Actions</strong></h3>

  <div class="alert alert-info" role="alert">
    <p>
      <span class="glyphicon glyphicon-question-sign" aria-hidden="true"></span>
      Create a body for a freshdesk response using an e-mail template
    </p>

    <p class="text-right">
      <%= link_to new_quote_emails_path(quote, freshdesk: true), class: 'btn btn-info btn-xs' do %>
          <i class="glyphicon glyphicon-print"></i> Prepare for FreshDesk
      <% end %>
    </p>
  </div>

  <div class="alert alert-info" role="alert">
    <p>
      <span class="glyphicon glyphicon-question-sign" aria-hidden="true"></span>
      Creating an order from a quote using this button will associate the order with the quote and populate
      relevant quote fields from the quote information.
    </p>

    <p class="text-right">
      <%= link_to new_order_path(quote_id: quote.id), class: 'btn btn-info btn-xs' do %>
          <i class="glyphicon glyphicon-plus-sign"></i> Create Order from Quote
      <% end %>
    </p>
  </div>

  <div class="alert alert-info" role="alert">
    <p>
      <span class="glyphicon glyphicon-question-sign" aria-hidden="true"></span>
      Will render a page that displays the quote information in a printable fashion.
    </p>

    <p class="text-right">
      <%= link_to quote_path(quote), class: 'btn btn-info btn-xs' do %>
          <i class="glyphicon glyphicon-print"></i> Print Quote
      <% end %>
    </p>
  </div>

</div>

<% imprintables.each do |imprintable| %>
  <hr>
  <div id="imprintable-result-<%=imprintable.id%>" class="imprintable-add-search-result row <%= 'discontinued-imprintable' if imprintable.discontinued? || imprintable.base_price.blank? %>">
    <div class="col-xs-2">
      <i><%= 'Standard Offering' if imprintable.standard_offering? %></i>
      <% imprintable.imprintable_imprintable_groups.each do |iig| %>
        <div> <b>Group:</b> <%= iig.imprintable_group.name %></div>
        <div> <b>Tier:</b> <%= Imprintable::TIERS[iig.tier] %></div>
      <% end %>
    </div>
      <div class="col-xs-6">
        <% if imprintable.discontinued? || imprintable.base_price.blank? %>
          <b><s><%= imprintable.name %></s></b><br />
          <s> <%= raw imprintable.description %></s>
        <% else %>
          <b><%= imprintable.name %></b><br />
          <%= raw imprintable.description %>
        <%end%>
      </div>
        <div class="col-xs-2">
          <b>Base Upcharge</b><br />
          <%= number_to_currency imprintable.base_price %>
        </div>
      <div class="col-xs-2">
        <b>Mark to Add</b><br />
        <% if imprintable.base_price.blank? %>
          Imprintable needs base price
        <% else %>
          <%= check_box_tag 'quote[line_item_to_group_attributes][imprintables][]', imprintable.id, local_assigns[:checked].try(:include?, imprintable.id) %>
        <% end %>
      </div>
  </div>
<% end %>

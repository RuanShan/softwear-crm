$("#contentModal .modal-body").html("<%= escape_javascript(render partial: 'shared/timeline', locals: {activities: @order.activities}) %>");
$("#contentModal .modal-title").html("Order #<%= escape_javascript @order.id.to_s %>");
$("#contentModal").modal("show");

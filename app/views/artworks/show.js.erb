showContentModal({
  backdrop: "static",
  keyboard: false,
  title: 'Artwork - <%= @artwork.name %>',
  body: $("<%= j(render partial: 'show',
                        locals: { artwork: @artwork }).html_safe %>")
});
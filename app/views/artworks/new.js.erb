showContentModal({
  backdrop: "static",
  keyboard: false,
  title: 'New Artwork',
  body: $("<%= j(render partial: 'new',
                        locals: { artwork: @artwork }).html_safe %>")
});

setSubmitTimeout();

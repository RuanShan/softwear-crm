showContentModal({
  backdrop: "static",
  keyboard: false,
  title: 'Edit Artwork Request',
  body: $("<%= j(render partial: 'edit',
                        locals: { artwork: @artwork }).html_safe %>")
});

setSubmitTimeout();

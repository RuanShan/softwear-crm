<%= form_for quote_request do |f| %>
  <%= f.select :quote_ids, [], {}, multiple: true, data: { values: quote_request.quotes.map { |q| { id: q.id, text: q.name } }.to_json } %>
  <%= f.submit class: 'btn btn-submit' %>
<% end %>

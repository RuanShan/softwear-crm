$imprint = $("<%= escape_javascript render partial: 'imprints/imprint', locals: { job: @job } %>");

$("#job-<%= @job.id %>")
  .find(".imprints-container")
  .append($imprint);

$imprint.find("select").addClass("editing-imprint");
$imprint.find(".js-delete-imprint-button").click(deleteImprint);

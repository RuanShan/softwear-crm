$('#contentModal .modal-body').html("<%= j(render 'prices/new', imprintable: @imprintable )
                                          .html_safe %>");
// immediately hide the pricing group text input
$('#pricing-group-input').hide();
$('#contentModal').modal('show');

showContentModal({
  backdrop: "static",
  keyboard: false,
  title: 'Edit Proof',
  body: $("<%= j(render partial: 'edit',
                        locals: { order: @order,
                                  proof: @proof }).html_safe %>")
});

initializeDateTimePicker();
setSubmitTimeout();
styleCheckboxes();
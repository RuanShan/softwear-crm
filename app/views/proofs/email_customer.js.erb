setupContentModal(function($contentModal){
  $contentModal.find('.modal-dialog').addClass('enlarge-modal')
});

showContentModal({
  backdrop: "static",
  keyboard: false,
  title: 'Proof Email',
  body: $("<%= escape_javascript(render partial: 'email_fields', locals: { order: @variable_hash[:order], proof: @variable_hash[:proof], reminder: @variable_hash[:reminder] }).html_safe %>")
});

initializeSummernote();
initializeProofSummernote();
setSubmitTimeout();
summernoteSubmit();

showContentModal({
  backdrop: "static",
  keyboard: false,
  title: 'New Proof',
  body: $("<%= escape_javascript(render(partial: 'new', locals: { order: @order, proof: @proof })).html_safe %>")
});

initializeDateTimePicker();
setSubmitTimeout();
setPendingSelect();
styleCheckboxes();
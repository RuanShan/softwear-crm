<% # NOTE the current implementation of this that is being used with dropzone is in global.js.coffee %>

<% if @fba_infos %>

  <% @fba_infos.each do |fba| %>
    console.log('ADDED <%=j fba[:filename] %>');
    var container = $("<%=j "#{render('fba_upload', filename: fba[:filename])}" %>");
    $('#js-packing-slip-info-zone').append(container);
    container.find('.info')
    .append("<%=j "#{render('fba_job_info_box', fba: fba)}" %>");
  <% end %>

  window.figureOutFbaOrderName();

<% else %>
  errorModal("le gasp");
<% end %>

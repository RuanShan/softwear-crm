<div class='box-info'>
<%= link_to 'Back', :back %>
<h3>Update your password</h3>
  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }, builder: LancengFormBuilder) do |f| %>
    <%= render_error_modal_for resource %>

    <%= render partial: 'users/password_fields', locals: { f:f } %>

    <div><%= f.submit "Update" %></div>
  <% end %>
</div>
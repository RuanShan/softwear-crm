<div class="page-heading animated fadeInDownBig col-lg-9 clear">
  <h1>Payment Drops <small>Make a Payment Drop for Today's Payments</small></h1>
</div>

<div class="jumbotron clear">
  <h3>
    <%= @undropped_payments.count %> Payments Are Ready To Be Dropped
  </h3>
  <%= link_to new_payment_drop_path, class: 'btn btn-primary' do  %>
      <i class="glyphicon glyphicon-plus-sign"></i> New Payment Drop
  <% end %>
</div>

<%= paginate @payment_drops %>

<div class="box-info row clear">
  <%= render partial: 'table', locals: { payment_drops: @payment_drops } %>
</div>

<%= paginate @payment_drops %>

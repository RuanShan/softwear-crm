<div class="box-info">
  <%= render partial: 'shared/modal_errors', locals: { object: brand } %>
  <%= form_for brand do |f| %>
    <!-- Horizontal form body -->
    <div id="horizontal-form" class="collapse in">
      <div class="form-group">
        <%= f.label :name, class: 'col-sm-2 control-label' %>
        <div class="col-sm-10">
          <%= f.text_field :name, class: 'form-control' %>
          <p class="help-block">The name of the brand</p>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :sku, class: 'col-sm-2 control-label' %>
        <div class="col-sm-10">
          <%= f.text_field :sku, class: 'form-control' %>
          <p class="help-block">SKU for this brand</p>
        </div>
      </div>

      <div class="form-group">
        <div class="col-sm-offset-2 col-sm-10">
          <button type="submit" class="btn btn-info">
            <%= create_or_edit_text(brand) %> Brand
          </button>
        </div>
      </div>

      <div class="form-group">
        <%= f.label :retail, class: 'col-sm-2 control-label' %>
        <div class="col-sm-10">
          <div class="radio">
            <%= f.radio_button :retail, false, value: false %>
            No
          </div>

          <div class="radio">
            <%= f.radio_button :retail, true, value: false %>
            Yes
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
disableFor('#delete-job-<%= @job.id %>', 1000);

<% if @job.destroyed? %>

  $("#job-<%= @job.id %>").fadeOut(500, updateOrderTimeline);

<% else %>
  
  errorModal("<%= j @job.errors.messages[:deletion_status].join(', ') %>");

<% end %>

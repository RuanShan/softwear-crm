$row = $('#artwork-request-<%= @artwork_request.id %>');
$tablerow = $('#artwork-request-row-<%= @artwork_request.id %>');
$tablerow.fadeOut();
$row.fadeOut();
$tablerow.remove();
$row.remove();
updateOrderTimeline();
setupContentModal(function($contentModal){
    $contentModal.find('.modal-content').addClass('modal-content-success')
})
showContentModal({
    title: 'SUCCESS',
    body: 'Successfuly destroyed Artwork Request!'
})
<h2>New Artwork Request For <%= link_to "Order ##{@order.id}", order_path(@order) %> "<%= @order.name %>"</h2>
<%= render partial: 'new', locals: { artwork_request: @artwork_request, order: @order } %>

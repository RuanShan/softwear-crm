showContentModal({
  backdrop: "static",
  keyboard: false,
  title: 'New Artwork Request',
  body: $("<%= j(render partial: 'new',
                        locals: { artwork_request: @artwork_request,
                                  order: @order }).html_safe %>")
});

initializeDateTimePicker();
initializeJobsChosen();
initializeSummernote();
setPendingSelect();
setSubmitTimeout();
summernoteSubmit();




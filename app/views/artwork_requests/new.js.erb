showContentModal({
  // backdrop: "static",
  keyboard: false,
  title: 'New Artwork Request',
  body: $("<%=j render partial: 'new', locals: { artwork_request: @artwork_request, order: @order } %>"),
  then: function() {
    initializeDateTimePicker();
    initializeSelect2();
    setPendingSelect();
    setSubmitTimeout();
    styleCheckboxes();
  }
});

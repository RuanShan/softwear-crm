<h2>Editing Artwork Request From <%= link_to "Order ##{@order.id}", order_path(@order) %> "<%= @order.name %>"</h2>
<%= render partial: 'edit', locals: { artwork_request: @artwork_request, order: @order } %>

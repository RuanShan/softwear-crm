showContentModal({
  // backdrop: "static",
  keyboard: false,
  body: $("<%= j(render partial: 'artwork_request',
                        locals: { artwork_request: @artwork_request,
                                  order: @order }).html_safe %>")
});
